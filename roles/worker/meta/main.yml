---
galaxy_info:
  role_name: worker
  author: luvis-sirpi
  description: Join Kubernetes worker nodes to the cluster
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions: [jammy, noble]
    - name: EL
      versions: ["9"]
dependencies:
  - role: kubernetes
