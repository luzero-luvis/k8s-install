---
collections:
  - name: ansible.posix       # sysctl, firewalld, authorized_key
    version: ">=1.5"
  - name: community.general   # dpkg_selections, timezone, modprobe, hostname
    version: ">=8.0"
